# Admin activity management (create / update / delete)

GitHub issue: https://github.com/cratharavi/skills-integrate-mcp-with-copilot/issues/10

**Description**

Add admin endpoints and (optional) UI to create, update and delete activities.

**Tasks**

- Add POST/PUT/DELETE endpoints for activities (admin-only)
- Add validation for fields (schedule, max participants, description)
- Add simple admin UI or API documentation examples
- Add tests for admin APIs

**Acceptance criteria**

- Admin can create/update/delete activities via API
- Validation works and returns meaningful errors
- Tests exist for admin endpoints

**Labels**: backend, frontend, feature
